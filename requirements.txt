# Swiggy Instamart case study — dependencies
# Versions pinned to exactly what was used to build and execute all 5 notebooks
# (01_master_data through 05_logistics_cost_benchmarking) with zero errors.
# Install with: pip install -r requirements.txt

# --- Analysis libraries used inside the notebook code ---
pandas==3.0.2
numpy==2.4.4
matplotlib==3.10.8
scipy==1.17.1

# --- Jupyter tooling needed to open and run the notebooks ---
jupyterlab==4.5.8
notebook==7.5.7
ipykernel==7.3.0
jupyter==1.1.1
jupyter_client==8.9.1
jupyter_core==5.9.1

# --- Needed only if you regenerate notebooks via build_notebook*.py
#     or re-execute them headlessly via `jupyter nbconvert --execute` ---
nbformat==5.10.4
nbconvert==7.17.1

# --- Additional ML & simulation libraries ---
# 06a (RL): gymnasium + stable-baselines3 (PPO); torch is the SB3 backend and is
#           imported explicitly in 06a, so it is pinned here rather than left transitive.
gymnasium==1.3.0
stable-baselines3[extra]==2.9.0
torch==2.12.1
tensorboard==2.20.0
# 06b (uplift): scikit-learn (X-Learner base models) + causalml (BaseXRegressor)
scikit-learn==1.9.0
causalml==0.16.0
# 06c (system dynamics + deep uncertainty): BPTK-Py + ema-workbench
BPTK-Py==1.6.2
ema-workbench==2.5.3
